export enum InventoryMovementType {
  ENTRADA = 'ENTRADA',
  SALIDA = 'SALIDA',
  AJUSTE = 'AJUSTE',
}

export enum InventoryOriginType {
  COMPRA = 'COMPRA',
  VENTA = 'VENTA',
  AJUSTE_MANUAL = 'AJUSTE_MANUAL',
}